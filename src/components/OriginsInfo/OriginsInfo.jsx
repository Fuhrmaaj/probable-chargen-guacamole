import React from 'react';
import styles from "./OriginsInfo.module.scss";

export default function OriginsInfo(props) {
    return (
        <div className="root">
            {props.children}
        </div>
    )
}
